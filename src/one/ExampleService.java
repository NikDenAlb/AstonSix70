package one;

public class ExampleService {
    static boolean checkField(String s) {
        return s.length() > 2;
    }

    static boolean checkField(int number) {
        return number > 0;
    }
}
